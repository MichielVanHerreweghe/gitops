apiVersion: apps/v1
kind: Deployment
metadata:
  name: jellyfin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jellyfin
  template:
    metadata:
      labels:
        app: jellyfin
    spec:
      nodeSelector:
        kubernetes.io/hostname: mediaserver
      containers:
        - name: jellyfin
          image: jellyfin/jellyfin:2025101305
          ports:
            - containerPort: 8096
          volumeMounts:
            - name: media
              mountPath: /media
          resources:
            requests:
              memory: "2Gi"
              cpu: "500m"
              gpu.intel.com/i915: "1"
            limits:
              memory: "4Gi"
              cpu: "1"
              gpu.intel.com/i915: "1"
      volumes:
        - name: media
          hostPath:
            path: /media/media/libraries
            type: Directory
